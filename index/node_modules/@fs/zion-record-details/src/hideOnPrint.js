import { css } from '@linaria/core'

// This is used for record-details printing
// The goal is to use CSS to keep the Ctrl + P print menu the same as the "Print Record" button
// This is a generic class that is used in many files for two main functions:
// 1. Always hide some things such as buttons, headers, and ui components
// 2. Only hide some things if the "fieldsToShowOnPrint" for them is set to false
export default css`
  @media print {
    display: none;
  }
`
