import React from 'react'
import { AuthRoute } from '@fs/zion-router'
import RouteCheck from './RouteCheck'

export default function TreeRoute({ requirePersonalData = false, ...props }) {
  return (
    <AuthRoute requirePersonalData={requirePersonalData} {...props}>
      <RouteCheck {...props} />
    </AuthRoute>
  )
}
