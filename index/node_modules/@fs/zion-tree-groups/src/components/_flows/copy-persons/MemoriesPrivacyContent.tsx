import React from 'react'
import { H2, Image, LayoutBand, Paragraph, Row, Separator } from '@fs/zion-ui'
import { useTranslation } from 'react-i18next'
import frame from '../../../assets/frame.png'

export default function MemoriesPrivacyContent(): JSX.Element {
  const [t] = useTranslation()

  return (
    <LayoutBand>
      <Row alignX="center">
        <Image src={frame} alt="" height="64px" width="64px" />
      </Row>
      <Separator size="xs" />
      <H2 size="H4" supplementary centered>
        {t('groups.memories-privacy.heading')}
      </H2>
      <Paragraph size="sm" centered>
        {t('groups.memories-privacy.description')}
      </Paragraph>
    </LayoutBand>
  )
}
