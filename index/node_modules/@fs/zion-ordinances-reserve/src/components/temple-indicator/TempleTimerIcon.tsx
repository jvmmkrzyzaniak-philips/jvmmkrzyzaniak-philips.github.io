import React from 'react'
import { Elevation } from '@fs/zion-ui/quarks-authorized-use-only'
import { css } from '@linaria/core'
import { atTextDirection } from '@fs/zion-ui'

export default function TempleTimerIcon({
  size,
}: {
  size?: string // in px
}): JSX.Element {
  return (
    <Elevation
      z={6}
      style={{
        borderRadius: '100%',
        width: size,
        height: size,
        background: 'var(--button-background)',
      }}
      className={css`
        &:hover {
          --button-background: var(--button-hover-background);
        }
        &:active {
          --button-background: var(--button-active-background);
        }
      `}
    >
      <svg
        width={size}
        height={size}
        viewBox="0 0 14 14"
        transform={atTextDirection({ ltr: undefined, rtl: `scale(-1, 1)` })}
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        style={{ display: 'block' }}
      >
        <circle cy="7" cx="7" r="5" fill="var(--button-background)" strokeWidth={1.5} stroke="currentcolor" />
        <path d="M7 4 L7 7.5 L9.5 8.5" stroke="currentcolor" strokeWidth={1.5} />
      </svg>
    </Elevation>
  )
}
