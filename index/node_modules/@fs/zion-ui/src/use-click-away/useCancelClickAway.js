import { useContext, useCallback } from 'react'
import { ClickAwayContext } from './ClickAwayProvider'

export default () => {
  const state = useContext(ClickAwayContext)

  const cancel = useCallback(() => {
    state.current.cancelled = true
  }, [state])

  return cancel
}
